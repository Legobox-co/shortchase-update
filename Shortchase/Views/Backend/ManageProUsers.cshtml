@model BackendPaidUserListDto
@{
    Layout = "_DashboardLayout";
}

<div class="header bg-gradient-primary py-3 ">
    <div class="container-fluid">
        <div class="header-body">
            <div class="row">
                <div class="col-sm-12 col-md-12 col-lg-12">
                    <h1 class="text-white text-left mb-0">
                        @*<i class="fas fa-user-tag"></i>*@
                        <img src="@Url.Content("~/img/newicons/pro.png")" class="img-fluid" width="40px" />&nbsp;
                        Pro Cappers
                    </h1>
                </div>
                @*<div class="col-sm-12 col-md-4 col-lg-4 text-right">
                        <button class="btn btn-info AddUsersModalBtn">
                            <i class="fas fa-plus"></i> Add
                        </button>
                    </div>*@
            </div>
        </div>
    </div>
</div>

<div class="pt-3">
    <div class="container-fluid">

        <div class="card shadow-sm">
            <div class="card-body py-3 px-3">
                @if (Model.Users == null || Model.Users.Count() <= 0)
                {
                    <p class="mb-0 text-small font-weight-900">
                        No pro cappers found on database.
                    </p>
                }
                else
                {
                    <div class="row">
                        <div class="col-sm-12 col-md-12 col-lg-12 text-right">
                            <button class="btn btn-dark-as-homepage btn-sm" id="SelectAllRowsBtn" data-power="select" data-totalselected="0" data-totalelements="@Model.Users.Count">Select All</button>
                            <button class="btn btn-success btn-sm batchaction" data-actiontype="switchstatus" data-status="true" data-action="@Url.Action("SwitchUsersStatusBatch", "Backend")"><i class="fas fa-power-off"></i> Activate All</button>
                            <button class="btn btn-warning btn-sm batchaction" data-actiontype="switchstatus" data-status="false" data-action="@Url.Action("SwitchUsersStatusBatch", "Backend")"><i class="fas fa-ban"></i> Deactivate All</button>
                            <button class="btn btn-light btn-sm batchaction" data-actiontype="switchsubscriptionstatus" data-status="true" data-action="@Url.Action("UpdateUserRenewSubscriptionBatch", "Backend")"><i class="fas fa-check"></i> Activate Selected Subscription Auto Renewal</button>
                            <button class="btn btn-dark btn-sm batchaction" data-actiontype="switchsubscriptionstatus" data-status="false" data-action="@Url.Action("UpdateUserRenewSubscriptionBatch", "Backend")"><i class="fas fa-ban"></i> Cancel Selected Subscription Auto Renewal</button>
                            <button class="btn btn-danger btn-sm batchaction" data-actiontype="delete" data-action="@Url.Action("DeleteUsersBatch", "Backend")"><i class="fas fa-trash"></i> Delete All</button>
                        </div>
                    </div>
                    <table id="UsersList" class="table table-striped table-bordered w-100" cellspacing="0">
                        <thead>
                            <tr>
                                <th class="text-uppercase"></th>
                                <th class="text-uppercase">First Name</th>
                                <th class="text-uppercase">Last Name</th>
                                <th class="text-uppercase">Email</th>
                                <th class="text-uppercase">Subscription Plan</th>
                                <th class="text-uppercase">Start Date</th>
                                <th class="text-uppercase">End Date</th>
                                <th class="text-uppercase">Status</th>
                                <th class="text-uppercase">Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.Users)
                            {

                                DateTime? orderOptionalDate = null;
                                if (item.LastSeen.HasValue)
                                {
                                    orderOptionalDate = item.LastSeen.Value;
                                }
                                <tr>
                                    <td id="SelectWrapper-@item.Id" class="rowSelectors text-center" data-controller="SelectAllRowsBtn" data-selected="false" data-itemid="@item.Id" data-subscriptionid="@item.ActiveSubscriptionId"></td>
                                    <td id="FirstName-@item.Id.ToString()" data-value="@item.FirstName">@item.FirstName</td>
                                    <td id="LastName-@item.Id.ToString()" data-value="@item.LastName">@item.LastName</td>
                                    <td id="Email-@item.Id.ToString()" data-value="@item.Email"><a href="mailto:@item.Email">@item.Email</a></td>
                                    <td id="Subscription-@item.Id.ToString()">@item.SubscriptionName</td>
                                    <td id="Start-@item.Id.ToString()">@DateHelper.DateFormat(item.SubscriptionStart.FromUTCData(Convert.ToInt32(ViewData["TimezoneOffset"].ToString())))</td>
                                    <td id="End-@item.Id.ToString()">@DateHelper.DateFormat(item.SubscriptionEnd.FromUTCData(Convert.ToInt32(ViewData["TimezoneOffset"].ToString())))</td>
                                    <td id="Status-@item.Id.ToString()">@item.SubscriptionStatus</td>
                                    <td>
                                        <a class="btn btn-sm btn-info ManageSubscriptionBtn timezoneoffset" asp-action="ManageProUserSubscriptions" asp-controller="Backend" asp-route-Id="@item.Id" data-toggle="tooltip" data-placement="top" title="Manage user subscriptions"><i class="fas fa-file-invoice-dollar"></i></a>
                                        @*<button class="btn btn-sm btn-primary EditItemBtn" data-id="@item.Id.ToString()" data-country-id="@item.PhoneCountry.Id" data-toggle="tooltip" data-placement="top" title="Edit"><i class="fas fa-edit"></i></button>*@

                                        @if (!item.IsActive)
                                        {
                                            <button class="btn btn-sm btn-success ActivateItemBtn" data-id="@item.Id" data-toggle="tooltip" data-placement="top" title="Activate"><i class="fas fa-power-off"></i></button>
                                        }
                                        else
                                        {
                                            <button class="btn btn-sm btn-warning DeactivateItemBtn" data-id="@item.Id" data-toggle="tooltip" data-placement="top" title="Deactivate"><i class="fas fa-power-off"></i></button>
                                        }
                                        <button class="btn btn-sm btn-danger DeleteItemBtn" data-id="@item.Id" data-toggle="tooltip" data-placement="top" title="Delete"><i class="fas fa-trash"></i></button>
                                        @if (item.AutoRenewal)
                                        {
                                            <button class="btn btn-sm btn-dark CancelAutoRenewalBtn" data-id="@item.ActiveSubscriptionId" data-toggle="tooltip" data-placement="top" title="Cancel Subscription Auto Renewal"><i class="fas fa-ban"></i></button>
                                        }
                                        else
                                        {
                                            <button class="btn btn-sm btn-light UnCancelAutoRenewalBtn" data-id="@item.ActiveSubscriptionId" data-toggle="tooltip" data-placement="top" title="Activate Subscription Auto Renewal"><i class="fas fa-check"></i></button>
                                        }
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                }
            </div>
        </div>

    </div>
</div>


<!-- Add Modal -->
@*<partial name="Partials/ManageUsers_AddModal" model="@Model" />
    <partial name="Partials/ManageUsers_EditModal" model="@Model" />*@


<input id="SwitchStatusUsersURL" hidden value="@Url.Action("SwitchStatusUsers", "Backend")" />
<input id="SoftDeleteUsersURL" hidden value="@Url.Action("SoftDeleteUsers", "Backend")" />
<input id="CancelUserRenewSubscriptionURL" hidden value="@Url.Action("CancelUserRenewSubscription", "Backend")" />
<input id="UnCancelUserRenewSubscriptionURL" hidden value="@Url.Action("UnCancelUserRenewSubscription", "Backend")" />

@section Scripts{
    <script src="@Url.Content("~/app/Backend/ManageProUsers.js")" asp-append-version="true"></script>
}