@model CurrencyListDto
@{
    Layout = "_DashboardLayout";
}

<div class="header bg-gradient-primary py-3 ">
    <div class="container-fluid">
        <div class="header-body">
            <div class="row">
                <div class="col-sm-12 col-md-8 col-lg-8">
                    <h1 class="text-white text-left mb-0">
                        @*<i class="fas fa-money-check-alt"></i>*@

                        <img src="@Url.Content("~/img/newicons/currency.png")" class="img-fluid" width="40px" />&nbsp;
                        Currencies
                    </h1>
                </div>
                <div class="col-sm-12 col-md-4 col-lg-4 text-right">
                    <button class="btn btn-dark-as-homepage AddCurrencyModalBtn">
                        <i class="fas fa-plus"></i> Add
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="pt-3">
    <div class="container-fluid">
        <div class="card shadow-sm">
            <div class="card-body py-3 px-3">
                @if (Model.Currencies == null || Model.Currencies.Count() <= 0)
                {
                    <p class="mb-0 text-small font-weight-900">
                        No currencies found on database.
                    </p>
                }
                else
                {
                    <div class="row">
                        <div class="col-sm-12 col-md-12 col-lg-12 text-right">
                            <button class="btn btn-dark-as-homepage btn-sm" id="SelectAllRowsBtn" data-power="select" data-totalselected="0" data-totalelements="@Model.Currencies.Count">Select All</button>
                            <button class="btn btn-danger btn-sm batchaction" data-actiontype="delete" data-action="@Url.Action("DeleteCurrencyBatch", "Backend")"><i class="fas fa-trash"></i> Delete All</button>
                        </div>
                    </div>
                    <table id="CurrenciesList" class="table table-striped table-bordered w-100" cellspacing="0">
                        <thead>
                            <tr>
                                <th class="text-uppercase"></th>
                                <th class="text-uppercase">Name</th>
                                <th class="text-uppercase">Acronym</th>
                                <th class="text-uppercase">Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.Currencies)
                            {
                            <tr>
                                <td id="SelectWrapper-@item.Id" class="rowSelectors text-center" data-controller="SelectAllRowsBtn" data-selected="false" data-itemid="@item.Id"></td>
                                <td id="Name-@item.Id" data-value="@item.Name">@item.Name</td>
                                <td id="Acronym-@item.Id" data-value="@item.Acronym">@item.Acronym</td>
                                <td>
                                    <button class="btn btn-sm btn-primary EditItemBtn" data-id="@item.Id" data-toggle="tooltip" data-placement="top" title="Edit"><i class="fas fa-edit"></i></button>
                                    @if (item.Deleted)
                                    {
                                        <button class="btn btn-sm btn-info ActivateItemBtn" data-id="@item.Id" data-toggle="tooltip" data-placement="top" title="Activate"><i class="fas fa-ban"></i></button>
                                    }
                                    else
                                    {
                                        <button class="btn btn-sm btn-danger DeleteItemBtn" data-id="@item.Id" data-toggle="tooltip" data-placement="top" title="Delete"><i class="fas fa-trash"></i></button>
                                    }


                                </td>
                            </tr>
                            }
                        </tbody>
                    </table>
                }
            </div>
        </div>

    </div>
</div>


<!-- Add Modal -->
<partial name="Partials/Currency_AddModal" model="@Model" />
<partial name="Partials/Currency_EditModal" model="@Model" />


<input id="DeleteCurrencyURL" hidden value="@Url.Action("DeleteCurrency", "Backend")" />
<input id="ActivateCurrencyURL" hidden value="@Url.Action("ActivateCurrency", "Backend")" />

@section Scripts{
    <script src="@Url.Content("~/app/Backend/Currencies.js")" asp-append-version="true"></script>
}